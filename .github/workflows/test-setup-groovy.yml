name: 'build-test-setup-groovy'

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'
  workflow_dispatch:

jobs:
  test-setup-groovy:
    uses: './.github/workflows/test.yml'
    with:
      module: 'setup-groovy'